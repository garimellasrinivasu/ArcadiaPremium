[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing — all routes serve index.html
[[redirects]]
  from = "/api/*"
  to = "https://arcadia-premium-api.onrender.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
